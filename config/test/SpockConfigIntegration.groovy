// Spock configuration for the `integrationTest` task.
//
// See config/test/SpockConfig.groovy: the script must exist because
// build.gradle sets the `spock.configuration` system property. The
// integrationTest task runs every tag, including `integration`.
runner {
}
